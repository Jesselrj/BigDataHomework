# 语义代码复用检测实验报告

## 摘要

本实验围绕语义代码复用检测（Semantic Code Reuse Detection），也即 Type-4 代码克隆检测，构建并评估了一个完整的研究型系统。系统同时支持两类任务：代码语义检索和代码对二分类。实验以 CodeXGLUE POJ-104 数据集为主要数据来源，将“解决同一编程题”的程序视为语义等价代码。方法上，实验实现了 TF-IDF 词法检索基线、CodeBERT 二分类器、GraphCodeBERT 二分类器、UniXcoder 双塔语义检索模型，以及“UniXcoder 检索 + GraphCodeBERT 重排序”的两阶段混合方法，并进一步构造随机负例、词法困难负例和长度/结构困难负例进行消融实验。

实验结果表明，单纯词法检索在 POJ-104 上能够取得一定效果，但明显弱于预训练代码模型。UniXcoder 双塔检索的 MAP@R 达到 0.9811，混合重排序方法进一步提升到 0.9816，是本实验中检索任务的最佳结果。分类任务中，GraphCodeBERT 的 F1 为 0.9170，略高于 CodeBERT 的 0.9117。困难负例消融中，加入困难负例后的混合方法 MAP@R 为 0.9794，较未加入时下降 0.0022，说明当前训练配置下困难负例未直接提升最终检索指标，但有助于暴露模型对词法相似负例和结构相似负例的鲁棒性问题。

关键词：语义代码复用检测；代码克隆检测；POJ-104；UniXcoder；GraphCodeBERT；CodeBERT；困难负例

## 1. 实验目的

本实验的目标是构建一个可复现的语义代码复用检测系统，用于识别不同程序之间是否存在语义等价或复用关系。与 Type-1/Type-2/Type-3 克隆主要关注文本、变量名或局部结构相似不同，Type-4 代码克隆更强调程序语义一致，即两段代码即使实现方式、变量命名和控制结构存在差异，只要解决相同问题，也应被识别为语义等价。

实验具体包含两个任务：

1. 代码语义检索：给定一个查询程序，从候选代码库中检索解决同一问题的程序。
2. 代码对二分类：给定两段代码，判断二者是否语义等价。

围绕上述任务，实验比较词法方法、预训练代码模型和两阶段混合方法的效果，并通过困难负例分析模型在高词法相似误匹配、低词法相似正例、长代码截断等情况下的表现。

## 2. 数据集与数据处理

实验使用 POJ-104 数据集。该数据集包含来自 104 个编程题的 C/C++ 程序，同一题目下的不同程序被视为语义等价代码。实验采用如下标注逻辑：

- 若 `problem_id` 相同，则构成正例，标签为 1。
- 若 `problem_id` 不同，则构成负例，标签为 0。

数据被处理为两种格式。第一种是检索格式，每条样本包含代码、样本 id、题目 id 和语言字段，用于语义检索任务。第二种是代码对格式，每条样本包含两段代码、两个题目 id、二分类标签和样本类型，用于 CodeBERT 与 GraphCodeBERT 的二分类训练和评估。

处理后的主要数据规模如下：

| 数据文件 | 样本数 |
|---|---:|
| `train.jsonl` | 32,500 |
| `validation.jsonl` | 8,500 |
| `test.jsonl` | 12,000 |
| `train_pairs.jsonl` | 26,000 |
| `validation_pairs.jsonl` | 6,800 |
| `test_pairs.jsonl` | 9,600 |
| `hard_negatives.jsonl` | 96,301 |

困难负例由三类样本组成：

| 困难负例类型 | 构造方式 | 数量 |
|---|---|---:|
| 随机负例 | 从不同 `problem_id` 中随机采样 | 32,500 |
| 词法困难负例 | 选择不同题目但 TF-IDF 相似度较高的代码对 | 31,301 |
| 长度/结构困难负例 | 选择不同题目但代码长度或 token 数相近的代码对 | 32,500 |

## 3. 方法设计

### 3.1 TF-IDF 词法检索基线

TF-IDF 基线首先对代码进行 token 化，将代码转化为词袋表示，再使用 TF-IDF 向量表示代码文本。检索阶段计算查询代码与候选代码之间的余弦相似度，并按相似度排序返回 Top-K 结果。该方法不使用深度模型，主要用于衡量词法相似性在语义代码检索中的基础效果。

### 3.2 CodeBERT 二分类模型

CodeBERT 二分类器使用 `microsoft/codebert-base` 作为编码器。输入形式为代码对拼接：

```text
code1 [SEP] code2
```

模型取编码后的分类表示，接一个线性分类头输出 clone / non-clone 概率。训练目标为交叉熵损失，评估指标包括 Accuracy、Precision、Recall、F1 和 AUC。

### 3.3 GraphCodeBERT 二分类与重排序模型

GraphCodeBERT 二分类器使用 `microsoft/graphcodebert-base` 作为编码器，输入和训练目标与 CodeBERT 相同。该模型在系统中承担两个角色：

1. 作为独立代码对二分类器。
2. 作为混合检索系统中的 Cross-Encoder 重排序器。

在重排序阶段，GraphCodeBERT 对 UniXcoder 检索得到的 Top-K 候选进行逐对打分，输出候选代码与查询代码的语义等价概率。

### 3.4 UniXcoder 双塔语义检索模型

UniXcoder 检索模型使用 `microsoft/unixcoder-base` 作为共享编码器，分别编码查询代码和候选代码：

```text
code -> UniXcoder -> vector
```

编码向量经过归一化后使用点积或余弦相似度计算匹配分数。训练时采用 in-batch negatives，即同一 batch 中其他样本作为负例，优化目标为 InfoNCE / Multiple Negatives Ranking Loss。该结构适合大规模候选池检索，因为所有候选代码可以预先编码并缓存。

### 3.5 UniXcoder + GraphCodeBERT 两阶段混合方法

最终系统采用两阶段设计：

1. 第一阶段使用 UniXcoder 双塔模型进行快速召回，取得 Top-100 候选。
2. 第二阶段使用 GraphCodeBERT Cross-Encoder 对候选进行重排序。

最终得分定义为：

```text
final_score = alpha * retrieval_score + beta * cross_encoder_score
```

本实验默认使用 `alpha = 0.4`，`beta = 0.6`。该设计兼顾了双塔模型的检索效率和 Cross-Encoder 的精细匹配能力。

## 4. 实验设置

实验在远端服务器 `h100` 上执行，项目路径为：

```bash
/home/lrj/BIG/semantic-code-reuse
```

实验使用新建 Conda 环境：

```bash
/home/lrj/envs/semantic-code-reuse
```

所有脚本均设置：

```bash
export CUDA_VISIBLE_DEVICES=3
```

日志显示 PyTorch 内部只可见 1 张 GPU，设备编号为 `cuda:0`。实际记录到的设备名称为 `NVIDIA A100-SXM-64GB`。Hugging Face 下载使用 `https://huggingface.co`；镜像站在服务器上不可达，因此按可连通性选择了官方站点。

主要训练配置如下：

| 配置项 | 取值 |
|---|---:|
| 随机种子 | 42 |
| 最大序列长度 | 512 |
| 训练轮数 | 3 |
| 学习率 | 2e-5 |
| 权重衰减 | 0.01 |
| warmup ratio | 0.1 |
| Cross-Encoder batch size | 可配置 |
| Dual-Encoder batch size | 可配置 |
| eval batch size | 可配置 |

## 5. 评价指标

检索任务使用如下指标：

- MAP@R：主要指标。对每个 query，令 R 为候选池中相关样本数量，计算前 R 个结果上的平均精度，再对所有 query 取平均。
- Recall@1、Recall@5、Recall@10：衡量前 K 个结果中是否召回相关样本。
- MRR：Mean Reciprocal Rank，衡量第一个相关结果的排名位置。

二分类任务使用如下指标：

- Accuracy：分类准确率。
- Precision：预测为正例的样本中真正例比例。
- Recall：全部正例中被正确识别的比例。
- F1：Precision 与 Recall 的调和平均。
- AUC：基于预测概率的 ROC-AUC。

## 6. 实验结果

### 6.1 总体结果

| 方法 | 任务 | MAP@R | Recall@1 | Recall@5 | Recall@10 | MRR | F1 | 说明 |
|---|---|---:|---:|---:|---:|---:|---:|---|
| TF-IDF | Retrieval | 0.5388 | 0.8077 | 0.9140 | 0.9487 | 0.8561 | - | 词法基线 |
| CodeBERT | Classification | - | - | - | - | - | 0.9117 | 代码对分类 |
| GraphCodeBERT | Classification | - | - | - | - | - | 0.9170 | 代码对分类 |
| UniXcoder | Retrieval | 0.9811 | 0.9977 | 0.9988 | 0.9992 | 0.9982 | - | 双塔检索 |
| UniXcoder + GraphCodeBERT | Retrieval + Rerank | 0.9816 | 0.9979 | 0.9989 | 0.9992 | 0.9984 | - | 混合方法 |
| Hybrid + Hard Negatives | Retrieval + Rerank | 0.9794 | 0.9978 | 0.9983 | 0.9986 | 0.9981 | - | 困难负例消融 |

从结果可以看出，TF-IDF 基线在 Recall@10 上达到 0.9487，说明词法相似性在 POJ-104 中仍然具有一定区分能力。但其 MAP@R 仅为 0.5388，说明排序质量和完整相关集合召回能力明显不足。UniXcoder 双塔模型将 MAP@R 提升到 0.9811，证明预训练代码模型能够显著增强语义检索能力。

混合方法在 UniXcoder 的基础上进一步引入 GraphCodeBERT 重排序，MAP@R 提升至 0.9816，Recall@1 提升至 0.9979，MRR 提升至 0.9984。虽然提升幅度不大，但在 UniXcoder 已经很强的前提下仍能带来稳定增益，说明 Cross-Encoder 对候选代码对的精细匹配仍有价值。

分类任务中，CodeBERT 的 F1 为 0.9117，GraphCodeBERT 的 F1 为 0.9170。GraphCodeBERT 略优于 CodeBERT，说明其预训练表示对代码对语义判断更有帮助。

### 6.2 困难负例消融实验

| 设置 | MAP@R | Recall@1 | Recall@5 | Recall@10 | MRR |
|---|---:|---:|---:|---:|---:|
| 不使用困难负例 | 0.9816 | 0.9979 | 0.9989 | 0.9992 | 0.9984 |
| 使用困难负例 | 0.9794 | 0.9978 | 0.9983 | 0.9986 | 0.9981 |
| 差值 | -0.0022 | -0.0001 | -0.0006 | -0.0006 | -0.0003 |

加入困难负例后，混合方法的 MAP@R 下降 0.0022，Recall 和 MRR 也出现轻微下降。这说明在当前训练轮数、采样比例和模型融合参数下，困难负例没有直接提升最终检索指标。可能原因包括：

1. 困难负例增加了训练难度，使模型在有限训练轮数内更难收敛到最优排序边界。
2. POJ-104 的测试集上 UniXcoder 召回已经非常强，进一步提升空间很小。
3. 困难负例的采样策略更强调词法和长度相似，可能引入了过多边界样本，使分类器更保守，从而影响部分正例排序。

不过，困难负例仍然具有分析价值。它能显式暴露模型在“不同语义但词法相似”场景下的误判风险，也为后续改进鲁棒性提供了训练数据基础。

## 7. 错误分析

错误分析脚本对混合方法预测结果进行了样本级检查，覆盖了六类典型情况。

第一类是高词法相似但语义不同的代码对。例如 `problem_4` 与 `problem_52`、`problem_10` 与 `problem_15` 中存在 TF-IDF 分数接近 1.0 的代码对，但它们实际解决不同问题。这说明仅依赖词法相似性容易把结构模板相似、变量和循环形式相近的程序误判为语义等价。

第二类是低词法相似但语义相同的代码对。例如 `problem_100` 中样本 `test_9553` 与 `test_9811` 的 token Jaccard 仅为 0.1518，但二者属于同一问题。这类样本说明语义等价代码可以采用完全不同的变量组织、分支写法和实现风格，词法方法难以稳定识别。

第三类是超长代码被截断的情况。部分 `problem_100` 样本 token 数超过 2,000，而模型最大长度为 512。长代码截断可能导致模型无法看到完整逻辑，尤其是当关键判断或输出逻辑出现在后半部分时，会影响分类和重排序。

第四类是同一问题下不同算法策略的实现。例如同一题目中既可能出现通用算法实现，也可能出现针对输入范围或特定样例的特殊写法。这类样本在 token overlap 上很低，但语义标签相同，是 Type-4 克隆检测的核心难点。

第五类是 TF-IDF 可能成功但神经模型失败的样本。错误分析中发现 GraphCodeBERT 对若干高 token Jaccard 的正例给出较低分数，例如 `problem_86` 中某正例对 token Jaccard 为 0.7609，但 GraphCodeBERT 分数仅为 0.1865。这说明神经模型并非总是优于词法方法，在部分高词面重合正例上也可能出现 false negative。

第六类是神经模型成功但 TF-IDF 失败的样本。例如 `problem_81` 中多个 query 的混合模型 Top-1 结果正确，而 TF-IDF Top-1 检索到了 `problem_82`、`problem_84` 或 `problem_85` 的代码。这说明预训练语义模型能够在一定程度上摆脱表层 token 相似性的限制。

总体来看，错误分析证明两阶段混合方法在大多数语义检索场景下表现稳定，但仍受限于输入长度、训练负例构造方式和 Cross-Encoder 对部分边界样本的判断能力。

## 8. 可复现性

完整实验代码位于远端服务器：

```bash
/home/lrj/BIG/semantic-code-reuse
```

复现实验可执行：

```bash
ssh h100
cd ~/BIG/semantic-code-reuse
source /home/lrj/.local/opt/miniconda3/etc/profile.d/conda.sh
conda activate /home/lrj/envs/semantic-code-reuse
export CUDA_VISIBLE_DEVICES=3
export HF_ENDPOINT=https://huggingface.co
pip install -r requirements.txt
bash scripts/run_all_experiments.sh
```

主要输出文件包括：

| 文件 | 内容 |
|---|---|
| `outputs/results/tfidf_results.json` | TF-IDF 检索结果 |
| `outputs/results/codebert_cls_results.json` | CodeBERT 分类结果 |
| `outputs/results/graphcodebert_cls_results.json` | GraphCodeBERT 分类结果 |
| `outputs/results/unixcoder_retrieval_results.json` | UniXcoder 检索结果 |
| `outputs/results/hybrid_rerank_results.json` | 混合重排序结果 |
| `outputs/results/hard_negative_ablation.json` | 困难负例消融结果 |
| `outputs/results/final_results.md` | 最终结果表 |
| `outputs/results/error_analysis.md` | 错误分析报告 |

实验验证中，`pytest -q` 的结果为：

```text
4 passed in 0.61s
```

## 9. 结论

本实验完成了一个从数据处理、模型训练、检索评估、分类评估到错误分析的语义代码复用检测系统。实验表明，预训练代码模型显著优于传统 TF-IDF 词法检索。UniXcoder 双塔模型能够高效完成语义召回，GraphCodeBERT Cross-Encoder 在重排序阶段进一步改善排序质量。最终混合方法在检索任务上取得 MAP@R 0.9816、Recall@1 0.9979、MRR 0.9984，是本实验中的最佳检索方案。

CodeBERT 和 GraphCodeBERT 在二分类任务上均取得较高 F1，其中 GraphCodeBERT 略优。困难负例消融没有带来最终指标提升，但揭示了模型在高词法相似负例和边界样本上的不足。后续工作可以从三方面改进：第一，引入显式数据流或控制流特征，增强对程序语义的建模；第二，优化困难负例采样比例和训练策略，避免模型过度保守；第三，在更多真实代码克隆数据集上验证模型的泛化能力。


## 10. 演示样例

本节给出若干可直接用于展示的真实样例。所有样例均来自已处理的 POJ-104 测试集或困难负例文件，模型分数来自已生成预测结果。为了便于阅读，代码片段做了截断。

### 10.1 语义检索成功示例

查询样本：`test_0`，题目：`problem_81`。混合模型排除自身后，Top-3 均来自同一题目。

查询代码片段：
```cpp
int jud(int n,int m){ if(n>=0&&n<=4&&m>=0&&m<=4) return 1; else return 0; } int main(){ int a[5][5],i,j,n,m,*p[5],*q; for(i=0;i<5;i++){ for(j=0;j<5;j++) scanf("%d",&a[i][j]); p[i]=a[i];} scanf("%d %d",&n,&m); if(jud(n,m)==0) printf("error"); else{ q=p[n]; p[n]=p[m]; p[m]=q; for(i=0;i<5;i++) for(j=0;j<5;j++){ if(j==4) printf("%d\n",*(p[i]+j)); else printf("%d ",*(p[i]+j));}} return 0;}
```
| Rank | candidate_id | problem_id | retrieval_score | cross_encoder_score | final_score | 是否同题 |
|---:|---|---|---:|---:|---:|---|
| 1 | `test_105` | `problem_81` | 0.9732 | 0.9991 | 0.9887 | 是 |
| 2 | `test_227` | `problem_81` | 0.9691 | 0.9989 | 0.9870 | 是 |
| 3 | `test_478` | `problem_81` | 0.9662 | 0.9990 | 0.9859 | 是 |

Top-1 候选代码片段：
```cpp
int check(int(*a)[5],int n,int m) { int temp,i; if(n>=0&&n<=4&&m>=0&&m<=4) { for(i=0;i<5;i++) { temp=*(*(a+m)+i); *(*(a+m)+i)=*(*(a+n)+i); *(*(a+n)+i)=temp; } return(1); } else return(0); } void main() { int (*a)[5]; int n,m,i,j; a=(int(*)[5])malloc(5*sizeof(int)); for(j=0;j<5;j++) { for(i=0;i<5;i++) { scanf("%d",*(a+j)+i); } } scanf("%d%d",&n,&m); if(check(a,n,m)==0) printf("error"); else { for(j=0;j<5;j++) { for(i= ...
```

### 10.2 TF-IDF 失败但混合模型成功示例

查询样本 `test_1` 属于 `problem_81`。TF-IDF Top-1 检索到 `test_1825`，属于 `problem_84`，语义不等价；混合模型 Top-1 检索到 `test_222`，属于同一题目 `problem_81`。

| 方法 | Top-1 candidate | candidate problem | 分数 | 是否正确 |
|---|---|---|---:|---|
| TF-IDF | `test_1825` | `problem_84` | 0.7598 | 否 |
| UniXcoder + GraphCodeBERT | `test_222` | `problem_81` | 0.9859 | 是 |

查询代码片段：
```cpp
int main(){ int A[5][5],m,n,row,list,i; row=0; while(row<5){ list=0; while(list<5){ scanf("%d",&A[row][list]); list++; } row++; } scanf("%d%d",&m,&n); if(m<0||n<0||m>4||n>4){ printf("error"); } else{ i=0; while(i<5){ row=A[m][i]; A[m][i]=A[n][i]; A[n][i]=row; i++; } row=0; while(row<5){ list=0; while(list<5){ if(list==4){ printf("%d\n",A[row][list]); } else{ printf("%d ",A[row][list]); } list++; } row++; } } return 0 ...
```
TF-IDF 误检候选片段：
```cpp
int main() { int i,j,k; int n; scanf("%d",&n); int list[n]; for (i=0; i<n; i++) { scanf("%d",&list[i]); } for (j=n-2;j>=n-3;j--) { for (k=0;k<=j;k++) { if (list[k] > list[k+1]) { list[k]=list[k]+list[k+1]; list[k+1]=list[k]-list[k+1]; list[k]=list[k]-list[k+1]; } } } printf("%d\n%d",list[n-1],list[n-2]); return 0; }
```
混合模型正确候选片段：
```cpp
int main() { int a[5][5],n,m,i,j,t,flag; for(i=0;i<5;i++) for(j=0;j<5;j++) scanf("%d",&a[i][j]); scanf("%d %d",&m,&n); if(m>=0&&m<=4&&n>=0&&n<=4) flag=1; else flag=0; if(flag==0) printf("error"); else { for(j=0;j<5;j++) { t=a[m][j]; a[m][j]=a[n][j]; a[n][j]=t; } for(i=0;i<5;i++) for(j=0;j<5;j++) { if(i==4&&j==4) printf("%d",a[4][4]); else if(j==4) printf("%d\n",a[i][j]); else printf("%d ",a[i][j]); } } return 0; }
```

### 10.3 GraphCodeBERT 二分类正例示例

样本 `pair_00006626`：`problem_97` vs `problem_97`，真实标签 1，预测标签 1，正例分数 0.9988。

代码 1：
```cpp
int main() { int m,c100=0,c50=0,c20=0,c10=0,c5=0,c1=0; cin>>m; if(m/100!=0) { c100=m/100; m=m%100; } if(m/50!=0) {c50=m/50; m=m%50; } if(m/20!=0) {c20=m/20; m=m%20; } if(m/10!=0) {c10=m/10; m=m%10; } if(m/5!=0) {c5=m/5; m=m%5; } if(m/1!=0) { c1=m; } cout<<c100<<endl<<c50<<endl<<c20<<endl<<c10<<endl<<c5<<endl<<c1<<endl; return 0; }
```
代码 2：
```cpp
int main(){ int a,b,c,d,e,f,g; scanf("%d",&a); b=(a-a%100)/100; a=a%100; c=(a-a%50)/50; a=a%50; d=(a-a%20)/20; a=a%20; e=(a-a%10)/10; a=a%10; f=(a-a%5)/5; a=a%5; g=a; printf("%d\n%d\n%d\n%d\n%d\n%d\n",b,c,d,e,f,g); return 0; }
```

### 10.4 GraphCodeBERT 二分类负例示例

样本 `pair_00006051`：`problem_96` vs `problem_102`，真实标签 0，预测标签 0，正例分数 0.0011。

代码 1：
```cpp
main() { char a[150]; char b [150]; int i,l,c,d,e,f; scanf("%s",a); l=strlen(a); if (l>2) { if((a[0]-'0')*10+(a[1]-'0')>=13) { b[0]=((a[0]-'0')*10+(a[1]-'0'))/13+'0'; c=(a[0]-'0')*10+(a[1]-'0'); for(i=1;i<=l-2;i++) { d=(b[i-1]-'0')*13; c=(c-d)*10+(a[i+1]-'0'); b[i]=c/13+'0'; } b[l-1]='\0'; f=c-(b[l-2]-'0')*13; printf("%s\n",b); printf("%d\n",f); } else { b[0]=((a[0]-'0')*100+(a[1]-'0')*10+(a[2]-'0'))/13+'0'; c=(a[0]- ...
```
代码 2：
```cpp
int main(){ int n,i,j,h=0,i1,j1,m,h1=0,i2,j2; char s[100][100]; double a[100],b[100]={0},c[100]={0},t1,t,min; scanf("%d",&n); for(i=0;i<n;i++){ scanf("%s%lf",s[i],&a[i]); } for(i=0;i<n;i++){ for(j=0;j<=1;j++){ if(s[i][j]=='m'){ b[h]=a[i]; h++; } } } for(i1=1;i1<=h;i1++){ for(j1=0;j1<h-i1;j1++){ if(b[j1]>b[j1+1]){ t1=b[j1]; b[j1]=b[j1+1]; b[j1+1]=t1; } } } for(i=0;i<n;i++){ for(j=0;j<=1;j++){ if(s[i][j]=='f'){ c[h1]=a ...
```

### 10.5 词法困难负例示例

该样例来自 `hard_negatives.jsonl`，类型为 `lexical_hard_negative`。两个程序题目不同，但 TF-IDF 相似度为 1.0000，容易干扰词法检索模型。

题目：`problem_4` vs `problem_52`

代码 1：
```cpp
int main() { int m,n,i,j,k,a[100][100]; scanf("%d%d",&m,&n); for(i=0;i<m;i++) for(j=0;j<n;j++) { scanf("%d",&a[i][j]); } printf("%d\n",a[0][0]); for(j=1;j<n;j++) { for(i=0;i<j+1&&i<m;i++) { printf("%d\n",a[i][j-i]); } } for(i=1;i<m;i++) { for(j=0;j<n&&j<m-i;j++)printf("%d\n",a[j+i][n-1-j]); } return 0; }
```
代码 2：
```cpp
int main() {int n, m, a[100], b[100], *p, i, j, t, k; scanf("%d%d",&n,&m); for(i=0;i<n;i++) {scanf("%d",&a[i]);} p=&a[n-m]; for(i=n-m;i<n;i++) {b[i-n+m]=*(p++);} for(j=0;j<m;j++) { for(i=j,t=a[i];i<n;i++) { k=a[i+1]; a[i+1]=t; t=k; } } for(i=0;i<m;i++) {a[i]=b[i];} printf("%d",a[0]); for(p=&a[1],i=1;i<n;i++) {printf(" %d",*(p++));} printf("\n"); return 0; }
```

### 10.6 神经模型漏判示例

样本 `pair_00000934` 是同题正例：`problem_83` vs `problem_83`，但 GraphCodeBERT 预测为 0，正例分数仅 0.0129。该例说明神经模型在部分边界样本上仍可能漏判。

代码 1：
```cpp
// // main.c // ??? // // Created by zhaoze on 13-11-7. // Copyright (c) 2013? zhaoze. All rights reserved. // void main() { int n,sum=0,i=0; int a[10]={0}; float GPA[10]={0.0},h=0.0; float tran(float n); scanf("%d",&n); for (i=0; i<n; i++) { scanf("%d",&a[i]); sum+=a[i]; } for (i=0; i<n; i++) { scanf("%f",&GPA[i]); GPA[i]=tran(GPA[i]); //printf("%.5f",GPA[i]); } for (i=0; i<n; i++) { h+=GPA[i]*a[i]; } printf("%.2f", ...
```
代码 2：
```cpp
float mark(int a) { if(a>=90&&a<=100) return(4.0); else if(a>=85&&a<=89) return(3.7); else if(a>=82&&a<=84) return(3.3); else if(a>=78&&a<=81) return(3.0); else if(a>=75&&a<=77) return(2.7); else if(a>=72&&a<=74) return(2.3); else if(a>=68&&a<=71) return(2.0); else if(a>=64&&a<=67) return(1.5); else if(a>=60&&a<=63) return(1.0); else if(a<=59) return(0); } int main() { int n; int all[10]={0}; int score[10]={0}; float ...
```
